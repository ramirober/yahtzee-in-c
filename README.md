## `yahtzee-in-c`

Little fun program for running yahtzee on the terminal made in C. Has multiple options and a full ruleset.
To run, compile with `gcc -Wall` (default options/flags should be enough)
